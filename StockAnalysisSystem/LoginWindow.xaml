<Window x:Class="StockAnalysisSystem.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="用户登录/注册" Height="550" Width="450"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">

    <Window.Resources>
        <!-- 卡片阴影效果 -->
        <DropShadowEffect x:Key="CardShadow"
                          BlurRadius="10"
                          ShadowDepth="2"
                          Opacity="0.15"
                          Color="#000000"/>

        <!-- 现代按钮样式（与主页面一致） -->
        <Style x:Key="ModernButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#1976D2"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Height" Value="40"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                CornerRadius="4"
                                Padding="8,4">
                            <ContentPresenter HorizontalAlignment="Center" 
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#1565C0"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#0D47A1"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- 链接按钮样式 -->
        <Style x:Key="LinkButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#2196F3"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <TextBlock Text="{TemplateBinding Content}"
                                   Foreground="{TemplateBinding Foreground}"
                                   TextDecorations="Underline">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Foreground" Value="#2196F3"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsMouseOver, RelativeSource={RelativeSource AncestorType=Button}}" Value="True">
                                            <Setter Property="Foreground" Value="#1565C0"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Background="#F5F5F5">

        <!-- 顶部标题栏 -->
        <Border  Background="#2196F3" Padding="20" Height="300" HorizontalAlignment="Right" Width="450">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                
            </StackPanel>
        </Border>

        <!-- 主内容区 -->
        <Border Margin="50,40,50,40"
                Background="White"
                CornerRadius="8"
                BorderBrush="#E0E0E0"
                BorderThickness="1"
                Effect="{StaticResource CardShadow}"
                Padding="40,35">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="20"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="20"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="20"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="30"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- 标题 -->
                <TextBlock x:Name="txtTitle"
                           Text="用户登录"
                           Grid.Row="0"
                           FontSize="24"
                           FontWeight="Bold"
                           Foreground="#212121"
                           HorizontalAlignment="Center"
                           Margin="0,0,0,10"/>

                <!-- 用户名输入框 -->
                <StackPanel Grid.Row="1">
                    <TextBlock Text="用户名" 
                               FontSize="13" 
                               Foreground="#757575" 
                               Margin="0,0,0,6"/>
                    <Border Background="White" 
                            CornerRadius="4" 
                            BorderBrush="#B3E5FC" 
                            BorderThickness="1"
                            Padding="12,8"
                            Height="40">
                        <TextBox x:Name="txtUsername" 
                                 BorderThickness="0"
                                 Background="Transparent"
                                 FontSize="14"
                                 VerticalAlignment="Center"
                                 Padding="0"/>
                    </Border>
                </StackPanel>

                <!-- 密码输入框 -->
                <StackPanel Grid.Row="3">
                    <TextBlock Text="密码" 
                               FontSize="13" 
                               Foreground="#757575" 
                               Margin="0,0,0,6"/>
                    <Border Background="White" 
                            CornerRadius="4" 
                            BorderBrush="#B3E5FC" 
                            BorderThickness="1"
                            Padding="12,8"
                            Height="40">
                        <PasswordBox x:Name="txtPassword" 
                                     BorderThickness="0"
                                     FontSize="14"
                                     VerticalAlignment="Center"
                                     Padding="0"/>
                    </Border>
                </StackPanel>

                <!-- 确认密码输入框（注册时显示） -->
                <StackPanel x:Name="spnConfirmPassword" 
                            Grid.Row="5" 
                            Visibility="Collapsed">
                    <TextBlock Text="确认密码" 
                               FontSize="13" 
                               Foreground="#757575" 
                               Margin="0,0,0,6"/>
                    <Border Background="White" 
                            CornerRadius="4" 
                            BorderBrush="#B3E5FC" 
                            BorderThickness="1"
                            Padding="12,8"
                            Height="40">
                        <PasswordBox x:Name="txtConfirmPassword" 
                                     BorderThickness="0"
                                     FontSize="14"
                                     VerticalAlignment="Center"
                                     Padding="0"/>
                    </Border>
                </StackPanel>

                <!-- 登录/注册按钮 -->
                <Button x:Name="btnSubmit"
                        Content="登录"
                        Grid.Row="7"
                        Style="{StaticResource ModernButtonStyle}"
                        Click="BtnSubmit_Click"/>

                <!-- 切换链接 -->
                <StackPanel Grid.Row="9" 
                            Orientation="Horizontal" 
                            HorizontalAlignment="Center"
                            Margin="0,10,0,0">
                    <TextBlock x:Name="txtSwitchHint"
                               Text="还没有账号？"
                               FontSize="12"
                               Foreground="#757575"
                               VerticalAlignment="Center"
                               Margin="0,0,5,0"/>
                    <Button x:Name="btnSwitch"
                            Content="立即注册"
                            Style="{StaticResource LinkButtonStyle}"
                            Click="BtnSwitch_Click"
                            Padding="0"
                            VerticalAlignment="Center"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- 底部状态栏 -->
        <StatusBar
                   VerticalAlignment="Bottom" 
                   Background="#F5F5F5" 
                   Padding="15,5"
                   Height="30">
            <StatusBarItem>
                <TextBlock x:Name="txtStatus" 
                           Text="就绪" 
                           FontSize="12" 
                           Foreground="#616161"/>
            </StatusBarItem>
        </StatusBar>
    </Grid>
</Window>

